# Etap 2 (AI) — optional overrides, only needed if you don't want the defaults.
# Local model via Ollama: no API key required.
# OLLAMA_MODEL=qwen2.5:14b-instruct
# OLLAMA_HOST=http://localhost:11434
